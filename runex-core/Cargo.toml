[package]
name = "runex-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
which = "7"
dirs = "6"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
